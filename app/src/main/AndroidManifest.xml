<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.steventanjung.gowork">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Go Work"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BarangActivity" />
        <activity android:name=".MenuActivity"/>
        <activity android:name=".MenuPegawaiActivity" />
        <activity android:name=".LihatBarangActivity" />
        <activity android:name=".TambahNota" />
        <activity android:name=".LihatNotaActivity"></activity>
    </application>

</manifest>